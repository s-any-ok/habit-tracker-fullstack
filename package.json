{
  "name": "habit-tracker-fullstack",
  "version": "1.0.0",
  "description": "Full-stack habit tracker with NestJS and React",
  "scripts": {
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm start",
    "build": "cd frontend && npm run build && cd ../backend && npm run build",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "format": "cd backend && npm run format && cd ../frontend && npm run format",
    "format:check": "cd backend && npm run format:check && cd ../frontend && npm run format:check"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
